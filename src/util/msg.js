'use strict';

/**
 * helper class for definition of telegram messages
 * @type {{chat: {id: string}, from: {username: string}}}
 */
let TelegramBotMessage = {
    chat: {
        id: ''
    },
    from: {
        username: ''
    },
};

module.exports = TelegramBotMessage;
